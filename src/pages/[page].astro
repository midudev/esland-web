---
import { pages, routes } from "@/components/StaticPaths.astro"

export async function getStaticPaths() {
  return routes;
}

export const prerender = true;

const { entryPoint } = Astro.props;
const Page = pages[entryPoint];
---
<Page />